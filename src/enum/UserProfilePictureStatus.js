'use strict';

/**
 * Enum for all the supported user profile picture loading status.
 *
 * @enum {string}
 * @readonly
 */
export default Object.freeze({
    LOADING: 'loading',
    FETCHED: 'fetched',
    ERROR: 'error'
});
